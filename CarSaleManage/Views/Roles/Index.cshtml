@using Microsoft.AspNetCore.Identity;
@model IEnumerable<IdentityRole>

@{
	ViewData["Title"] = "Roles";
}

<h1>Roles</h1>

@* <form method="post" asp-action="Create" asp-controller="Roles"> *@
@* 	<div class="input-group"> *@
@* 		<input name="identityRole" class="form-control w-25"> *@
@* 		<span class="input-group-btn"> *@
@* 			<button class="btn btn-primary">Add New Role</button> *@
@* 		</span> *@
@* 	</div> *@
@* </form> *@

<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(Model => Model.Id)
			</th>
			<th>
				@Html.DisplayNameFor(Model => Model.Name)
			</th>			
		</tr>
	</thead>
	<tbody>
		@foreach(var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem=>item.Id)
				</td>
				<td>
					@Html.DisplayFor(modeltem=>item.Name)
				</td>

			</tr>
		}
	</tbody>
</table>

<a class="btn btn-primary" asp-controller="UserRoles" asp-action="Index" >User Role Manageer</a>